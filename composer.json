{
    "name": "wafaaelcadi/gestion-formation",
    "description": "$validation = $validator->validate($inputs, $rules);",
    "require": {
        "rakit/validation": "^1.1"
        // dir cmd+o
    }
}
